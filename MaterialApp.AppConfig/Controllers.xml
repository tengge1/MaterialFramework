<objects xmlns="http://www.springframework.net">

  <!-- 角色菜单 -->
  <object type="MaterialApp.Controller.RoleMenuController, MaterialApp.Controller" singleton="false">
    <property name="bll" ref="RoleMenuBLL" />
  </object>

  <!-- 登录 -->
  <object type="MaterialApp.Controller.LoginController, MaterialApp.Controller" singleton="false">
    <property name="bll" ref="LoginBLL" />
  </object>

  <!-- 菜单 -->
  <object type="MaterialApp.Controller.MenuController, MaterialApp.Controller" singleton="false">
    <property name="bll" ref="MenuBLL" />
  </object>

  <!-- 角色 -->
  <object type="MaterialApp.Controller.RoleController, MaterialApp.Controller" singleton="false">
    <property name="bll" ref="RoleBLL" />
  </object>

  <!-- 配置 -->
  <object type="MaterialApp.Controller.ConfigController, MaterialApp.Controller" singleton="false">
    <property name="bll" ref="ConfigBLL" />
  </object>

  <!-- 配置节点 -->
  <object type="MaterialApp.Controller.ConfigSectionController, MaterialApp.Controller" singleton="false">
    <property name="bll" ref="ConfigSectionBLL" />
  </object>

  <!-- 字典 -->
  <object type="MaterialApp.Controller.DicController, MaterialApp.Controller" singleton="false">
    <property name="bll" ref="DicBLL" />
  </object>

  <!-- 字典项 -->
  <object type="MaterialApp.Controller.DicItemController, MaterialApp.Controller" singleton="false">
    <property name="bll" ref="DicItemBLL" />
  </object>

  <!-- 日志 -->
  <object type="MaterialApp.Controller.LogController, MaterialApp.Controller" singleton="false">
    <property name="bll" ref="LogBLL" />
  </object>

  <!-- 机构 -->
  <object type="MaterialApp.Controller.DeptController, MaterialApp.Controller" singleton="false">
    <property name="bll" ref="DeptBLL" />
  </object>

  <!-- 用户 -->
  <object type="MaterialApp.Controller.UserController, MaterialApp.Controller" singleton="false">
    <property name="bll" ref="UserBLL" />
  </object>

  <!-- 数据库备份 -->
  <object type="MaterialApp.Controller.DatabaseBackupController, MaterialApp.Controller" singleton="false">
    <property name="bll" ref="DatabaseBackupBLL" />
  </object>

  <!-- 工作流 -->
  <object type="MaterialApp.Controller.WorkflowController, MaterialApp.Controller" singleton="false">
    <property name="bll" ref="WorkflowBLL" />
  </object>

  <!-- 文件 -->
  <object type="MaterialApp.Controller.FilesController, MaterialApp.Controller" singleton="false">
    <property name="bll" ref="FilesBLL" />
  </object>

  <!-- 消息 -->
  <object type="MaterialApp.Controller.MessageController, MaterialApp.Controller" singleton="false">
    <property name="bll" ref="MessageBLL" />
  </object>

  <!-- 消息接收 -->
  <object type="MaterialApp.Controller.MessageReceiveController, MaterialApp.Controller" singleton="false">
    <property name="bll" ref="MessageReceiveBLL" />
  </object>

  <!-- 请假 -->
  <object type="MaterialApp.Controller.LeaveController, MaterialApp.Controller" singleton="false">
    <property name="bll" ref="LeaveBLL" />
  </object>
</objects>

